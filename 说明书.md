# 说明书

## 一、技术领域

本发明涉及人工智能、机器学习、数据可视化与人机交互等技术领域，具体是一种面向群体公平性问题的交互式可视分析框架，适用于高维多属性数据（如人脸图像数据集）中机器学习模型的公平性分析、去偏优化及决策解释。

---

## 二、背景技术

随着人工智能技术的广泛应用，机器学习模型在金融、医疗、司法等敏感领域的决策影响日益增强。然而，由于训练数据中的群体分布不均及模型本身的结构特点，常常导致对不同敏感属性群体（如性别、年龄、种族等）在预测输出上存在显著的公平性问题。

### 主要挑战包括：

1. **去偏优化过程缺乏直观性**：现有算法多为黑盒过程，难以理解和调节公平性与准确率的权衡。
2. **缺乏交互式、动态可视分析工具**：大部分工具仅能静态展示结果，无法实时反馈不同去偏策略的影响。
3. **模型决策可解释性不足**：难以溯源偏见产生的根本机制，阻碍对模型决策依据的信任。
4. **多属性高维数据的复杂性**：如人脸图像等，属性间存在复杂统计和因果关系，现有单一敏感属性去偏策略难以充分解决公平性问题。

因此，亟需一种能够支持多属性协同、动态优化、多层次评估与决策解释的交互式可视分析框架。

---

## 三、发明内容

### 1. 发明目的

本发明旨在提供一种集属性关联分析、动态去偏优化、多层次公平性评估及可解释决策分析为一体的可视分析框架，解决现有技术不足，提升高维数据场景下机器学习系统的公平性、透明度与可控性。

---

### 2. 技术方案

#### 2.1 总体结构

所述可视分析框架包括如下主要模块：

- **属性关联探索模块**
- **动态模型去偏优化模块**
- **多层次公平性评估与解释模块**
- **前后端协同的系统实现架构**

#### 2.2 属性关联探索模块

该模块针对高维多属性数据集，计算并可视化各属性与敏感属性之间的相关性。主要用到以下统计公式：

1. **Phi系数（φ）**  
   用于衡量两个二元属性之间的相关性，定义如下：  

   $$
   \phi = \frac{AD - BC}{\sqrt{(A+B)(C+D)(A+C)(B+D)}}
   $$

   其中，$A$、$B$、$C$、$D$分别为如下属性联合分布的样本数：  
   - $A$：属性1=True，属性2=True  
   - $B$：属性1=True，属性2=False  
   - $C$：属性1=False，属性2=True  
   - $D$：属性1=False，属性2=False

2. **优势比（Odds Ratio, OR）**  
   用于衡量两个二元属性共现概率的关联性：  

   $$
   OR = \frac{A \cdot D}{B \cdot C}
   $$

   当$OR > 1$时，两个属性正相关；$OR < 1$时负相关；$OR \approx 1$时表明属性独立。

通过上述指标，系统自动生成相关性矩阵和混淆矩阵，结合表格、热力图等方式直观展示属性间关系。

#### 2.3 动态模型去偏优化模块

该模块集成生成对抗网络（GAN）等去偏算法，实现对敏感属性的可控数据增强和分布均衡。具体步骤如下：

1. **生成对抗网络潜空间调整**  
   对于给定的潜向量$z$，定义敏感属性分类器权重$\mathbf{w}_g$及目标属性分类器权重$\mathbf{w}_t$，构造如下区分判别函数：

   $$
   f_g(z) = \mathbf{w}_g^T z + b_g \\
   f_t(z) = \mathbf{w}_t^T z + b_t
   $$

   通过优化潜空间调整后的$z'$，使得目标属性输出$f_t(z') \approx f_t(z)$，而敏感属性输出$f_g(z')$发生改变，从而生成在目标属性保持不变而敏感属性变化的新样本：

   $$
   z' = z + \lambda \left( \frac{\mathbf{w}_t^T z + b_t}{\|\mathbf{w}_t\|^2} \mathbf{w}_t - \frac{\mathbf{w}_g^T z + b_g}{\|\mathbf{w}_g\|^2} \mathbf{w}_g \right)
   $$

   其中$\lambda$为调节系数。

2. **数据分布可视化与动态调整**  
   系统采用交互式滑动条等控件，允许用户动态调整真实数据与生成数据的比例，实时观测各组合下属性分布、相关性和公平性指标的变化。  
   相关指标实时更新，包括：

   - **类别占比（Category Distribution）**  
     $$
     p_{cat} = \frac{N_{cat}}{N_{total}}
     $$
     其中$N_{cat}$为某类别样本数，$N_{total}$为总样本数。

   - **相关性与公平性指标**  
     实时展示$\phi$、$OR$、DEO和BA等随数据分布变化的趋势。

#### 2.4 多层次公平性评估与解释模块

该模块支持从宏观统计到微观实例的多层次分析：

1. **公平性衡量指标**  
   - **机会均等差异（Difference in Equality of Opportunity, DEO）**  
     衡量不同敏感群体假负率（FNR, False Negative Rate）差异，定义为：

     $$
     DEO = |FNR_{S=0} - FNR_{S=1}|
     $$
     其中$FNR_{S=s}$为敏感属性$s$下的假负率：
     $$
     FNR_{S=s} = 1 - \frac{TP_{S=s}}{TP_{S=s} + FN_{S=s}}
     $$

   - **偏差放大（Bias Amplification, BA）**  
     衡量模型预测结果在敏感群体中的偏差放大程度：

     $$
     BA = \max_s |\hat{P}_{t|s} - P_{t|s}|
     $$

     其中$\hat{P}_{t|s}$表示模型预测下，给定敏感属性$s$时目标属性$t$的概率，$P_{t|s}$为真实分布概率。

2. **嵌入空间可视化**  
   系统支持PCA、t-SNE、UMAP等降维算法，将高维特征映射至二维空间，投影点用不同颜色和形状编码敏感属性和目标类别，便于宏观洞察数据分布及潜在偏见。

3. **模型可解释性分析**  
   - **Grad-CAM（Gradient-weighted Class Activation Mapping）**  
     利用卷积神经网络最后一个卷积层的特征图$\mathbf{A}^k$及其梯度$\alpha_k^c$，定义类别$c$的热力图为：

     $$
     L_{Grad-CAM}^c = ReLU\left(\sum_k \alpha_k^c \mathbf{A}^k\right)
     $$
     其中
     $$
     \alpha_k^c = \frac{1}{Z} \sum_i \sum_j \frac{\partial y^c}{\partial A_{ij}^k}
     $$
     $ReLU$保证只考虑正向贡献。

   - **积分梯度（Integrated Gradients, IG）**  
     衡量输入特征对输出结果的敏感性，定义为：

     $$
     IG_i(x) = (x_i - x_i') \cdot \int_{\alpha=0}^1 \frac{\partial F(x'+\alpha \cdot (x-x'))}{\partial x_i} d\alpha
     $$
     其中$x'$为参考基线输入，$F(\cdot)$为模型输出。

4. **多视图联动与交互**  
   - 宏观视图：数据分布、指标统计；  
   - 微观视图：单样本决策依据展示、相似样本对比、筛选与聚焦局部群体。

---

### 3. 系统实现

#### 3.1 前端

- 基于Vue.js开发，采用Echarts与d3.js等库实现多模块、多视图的交互式可视化；
- 提供属性选择、滑动条调整、降维方式切换、条件筛选等丰富交互控件；
- 支持大规模数据的高效渲染与动态加载。

#### 3.2 后端

- 基于Python Flask实现API接口；
- 基于PyTorch实现ResNet-50等主流深度学习模型的训练、推理与去偏样本生成；
- 实现Phi、OR、DEO、BA等统计与公平性指标的计算；
- 支持批量数据处理、模型预测与中间结果的缓存。

#### 3.3 前后端交互

- RESTful接口传递数据、模型结果与用户指令；
- JSON格式标准化数据结构，保障系统的可扩展性与健壮性。

---

## 四、附图说明

- **图1**：系统整体架构流程图
- **图2**：属性关联探索模块界面/流程示意
- **图3**：动态模型去偏优化模块界面/流程示意
- **图4**：多层次公平性评估与解释模块界面/流程示意
- **图5**：关键公式推导与指标计算流程图

---

## 五、具体实施方式

### 1. 数据集与基础模型

以CelebA人脸图像数据集为例，包含约20万张人脸图像，每张标注40个二元属性，适合于属性分析和公平性研究。基础模型为ResNet-50卷积神经网络，训练参数如：学习率0.001、训练50轮、batch size 32、优化器Adam。

### 2. 属性相关性分析与可视化

- 对每对属性，计算Phi系数与优势比（见上述公式），结果填入相关性矩阵。
- 用户可通过界面选择敏感属性（如性别），系统自动高亮与其相关性大的属性，并以热力图、混淆矩阵等方式可视化分布。
- 例：性别与眼镜属性Phi=0.418，优势比=2.03，存在显著相关。

### 3. 动态数据增强与去偏优化

- 用户选择需去偏的属性组合（如性别-眼镜），系统根据GAN潜空间调整生成新样本，调节生成与真实数据比例。
- 每次调整后，系统实时更新属性分布、Phi、OR、DEO、BA等指标（见公式），以雷达图和柱状图反馈效果。
- 用户多轮实验后选取公平性与准确率兼顾的数据方案。

### 4. 多层次公平性评估与解释

- 宏观视图：降维投影（PCA/t-SNE/UMAP）展示全局数据分布，颜色区分属性、形状区分类别，统计准确率、DEO、BA等变化。
- 微观视图：筛选特定样本（如男性戴眼镜/女性不戴眼镜），展示Grad-CAM与积分梯度解释结果，分析模型关注区域与推理逻辑，对比相似样本的决策一致性。

### 5. 系统评估与优化

- 案例分析：邀请用户实际操作属性筛选、去偏调整、公平性评估等流程，记录其反馈与操作建议。
- 专家评估：邀请领域专家多角度评价系统流程、功能与可视化设计，并提出改进建议。

---

## 六、实施效果与应用前景

- 能够帮助用户动态发现和优化模型在多属性高维数据下的群体公平性问题；
- 显著提升模型公平性分析的效率和深度，提升AI系统在敏感场景下的可信度；
- 框架可扩展至视频、多模态数据等更复杂场景和多种深度学习任务。

---

本说明书结合具体公式、实施流程和系统架构，详尽阐述了本发明的创新点与实际操作方法，能够使本领域技术人员据此顺利实现本发明，并推动机器学习公平性分析与优化的发展。